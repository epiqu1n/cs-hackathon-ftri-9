(async () => {
/*
  // For importing component
  const contentImageIrl = chrome.runtime.getURL("components/ContentImage.js");
  const ContentImage = (await import(contentImageIrl)).default;
 */

/* 
  // Replace 'contents' element with custom image
  const contents = $("#contents");
  const contentImg = new ContentImage();
  contents.replaceWith(contentImg.img);
 */

/* 
  // Add text before image
  const text = $("<span>Get back to work!</span>").addClass("beautText");
  text.insertBefore(contentImg.img);
 */
  chrome
})();
